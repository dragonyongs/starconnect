import React from "react";
import Button from '../../components/Button';

const ProfilePage = () => {
    return (
        <div>
            <h1>profile Page</h1>
            <Button type="submit" className="">로그아웃</Button>
        </div>
    );
}

export default ProfilePage;